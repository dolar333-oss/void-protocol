{
  "openapi": "3.0.0",
  "info": {
    "title": "VOID x402 Gateway",
    "version": "1.0.0",
    "description": "USDC per-call services for AI agents"
  },
  "paths": {
    "/v1/check_contract": {
      "post": {
        "summary": "Check contract safety",
        "parameters": [{"name": "agent", "in": "query", "required": true, "schema": {"type": "string"}}],
        "requestBody": {
          "content": {"application/json": {"schema": {"type": "object"}}}
        },
        "responses": {"200": {"description": "Result"}}
      }
    }
  }
}
